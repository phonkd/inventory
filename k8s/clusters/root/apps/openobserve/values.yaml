ingester:
  persistence:
    storageClass: "proxmox-data"

querier:
  persistence:
    storageClass: "proxmox-data"

alertmanager:
  persistence:
    storageClass: "proxmox-data"

nats:
  config:
    jetstream:
      fileStore:
        pvc:
          storageClassName: "proxmox-data"

config:
  ZO_S3_SERVER_URL: "https://api.s3.w.phonkd.net"
  ZO_S3_BUCKET_NAME: "openobserve"
  ZO_S3_FEATURE_FORCE_HOSTED_STYLE: "false"
  ZO_S3_REGION_NAME: "us-east-1"

postgres:
  spec:
    storage:
      storageClass: "proxmox-data"
externalSecret:
  enabled: true
  name: "openobserve-s3-creds"
