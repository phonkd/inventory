apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  # Specify a name
  name: proxmox-csi-config
  annotations:
    config.kubernetes.io/function: |
      exec:
        path: ksops
files:
  - ./secret.enc.yaml
